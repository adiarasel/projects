#include <iostream>
#include <fstream>
#include <algorithm>
#include <cmath>
#include <string>

using namespace std;

//Declare variables.
//MAKE THEM GLOBAL so they can be accessed in functions if any

//any functions if needed

int main()
{
    ifstream fin("CHANGEME.in");
    ofstream fout("CHANGEME.out");
    
    //Read the varaiables
    // DO NOT use cin, use fin instead


    // insert logic here


    //fout output make sure formatting is correct

    //close the program

    fin.close();
    fout.close();
    return 0;
}