#include <iostream>
#include <algorithm>
#include <cmath>
#include <fstream>
#include <string>
#include <vector>
#include <stack>
#include <map>
#include <set>
#include <queue>
#define FOR(i, n) for(int i=0; i<n; i++)
using namespace std;
int n, a, b, x[10000], y[10000];
char ch[10000];
int dy[4] = {1, 0, 0, -1};
int dx[4] = {0, -1, 1, 0};
int main()
{
    cin>>n>>a>>b;
    FOR(i, n){
        cin>>x[i]>>y[i]>>ch[i];
    }
    
}