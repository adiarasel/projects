#include <iostream>
#include <fstream>
#include <algorithm>
#include <cmath>
#include <string>
#include <vector>
#include <stack>
#include <map>
#include <set>
#include <queue>
#define FOR(i, n) for(int i=0; i<n; i++)
using namespace std;

//Declare variables.
//MAKE THEM GLOBAL so they 
//can be accessed in functions if any

//any functions if needed

int main()
{
    ifstream fin("CHANGE ME.in");
    ofstream fout("CHANGE ME.out");
    
    //Read the varaiables
    // DO NOT use cin, use fin instead

    // insert logic here

    //fout output make sure formatting is correct

    //close the program

    fin.close();
    fout.close();
    return 0;
}