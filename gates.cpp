#include <iostream>
#include <algorithm>
#include <cmath>
#define FOR(i, n) for(int i=0; i<n; i++)
using namespace std;
int n;
string s;
bool fences[4000][4000], visited[4000][4000];
int row = 2000, col = 2000;
int main()
{
    cin>>n>>s;
    FOR(i, n){
        if(s[i] == 'N'){// north

        }
    }
}